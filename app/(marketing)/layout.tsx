import { marketingHomeMetadata } from "@/lib/seo"

export const metadata = marketingHomeMetadata

export default function MarketingLayout({ children }: { children: React.ReactNode }) {
  return children
}
